#set($hash = '#')
${hash} TESTING ONLY

server.port=8080

server.servlet.context-path=/${resource-alias}

spring.application.name = user-service-server

spring.mvc.pathmatch.matching-strategy = ant-path-matcher